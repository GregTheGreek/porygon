"""poryml - deterministic primitives for AI-augmented pokeemerald ROM hacking.

The `core` package is a pure library with no MCP/Claude coupling so it can be
tested and used standalone. `cli` and `server` are thin wrappers over it.
"""

__version__ = "0.1.0"
